<main>
    {{#each projects}}
        <section>
            <p><a href="{{url}}" target="_blank">{{name}}</a></p>
            <details>
                <summary>{{forkCount}} forks</summary>
                {{#each forks.nodes}}
                    <section>
                        <p><a href="{{url}}" target="_blank">{{name}}</a></p>
                        <p><a href="{{owner.url}}" target="_blank">{{owner.login}}</a></p>
                        <img src="{{owner.avatarUrl}}" alt="{{owner.login}}">
                    </section>
                {{/each}}
            </details>
        </section>
    {{/each}}
</main>